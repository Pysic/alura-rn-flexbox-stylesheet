import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create(
    {
        shape: { flexDirection: 'row', borderColor: '#22D4FD', borderWidth: 2, borderRadius: 8, padding: 23, alignItems: 'center' , justifyContent: 'center', gap: 16 },
        text: { color: '#F6F6F6', fontFamily: 'Montserrat-SemiBold', fontSize: 22, lineHeight: 24 * 1.2 }
    }
)